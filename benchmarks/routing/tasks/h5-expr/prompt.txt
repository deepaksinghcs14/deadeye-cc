Create a new Go package at internal/expr/expr.go (package expr) with `func Eval(s string) (int, error)` that evaluates an integer arithmetic expression. Support + - * / , parentheses, and unary minus, with standard precedence (* and / bind tighter than + and -) and left-to-right associativity for equal precedence. Division is integer division truncating toward zero. Whitespace is ignored. Return a non-nil error for division by zero and for any malformed input (unbalanced parentheses, trailing operator, empty expression, unexpected characters). Add a one-line package doc comment.
