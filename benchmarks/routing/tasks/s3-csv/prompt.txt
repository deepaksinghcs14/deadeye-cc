Create a new Go package at internal/csvx/csvx.go (package csvx) with `func ParseLine(line string) []string` parsing a single RFC4180-style CSV record. Fields are comma-separated. A field wrapped in double quotes may contain commas, and a doubled double-quote ("") inside a quoted field is one literal double-quote. Unquoted fields preserve their leading/trailing spaces exactly. Empty fields are allowed (so "a,,c" yields three fields, the middle one empty), and a trailing comma yields a trailing empty field. Return each field with surrounding quotes removed and escapes resolved. Add a one-line package doc comment.
