Create a new Go package at internal/counter/counter.go (package counter) with a concurrency-safe key counter: `func New() *Counter`, method `Add(key string)` incrementing that key's count by one, and method `Total(key string) int` returning the current count (zero for an unseen key). It must be correct and race-free under many goroutines calling Add concurrently (must pass `go test -race`, and totals must be exact with no lost updates). Add a one-line package doc comment.
